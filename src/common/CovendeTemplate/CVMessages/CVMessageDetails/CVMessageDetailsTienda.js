import React from 'react';
import { Box } from '@chakra-ui/react';

function CVMessageDetailsTienda({ model }) {
  return <Box>{JSON.stringify(model)}</Box>;
}

export default CVMessageDetailsTienda;
