import { Box } from '@chakra-ui/react';
import React from 'react';

function CVMessageDetailsPedido({ model }) {
  return <Box>{JSON.stringify(model)}</Box>;
}

export default CVMessageDetailsPedido;
