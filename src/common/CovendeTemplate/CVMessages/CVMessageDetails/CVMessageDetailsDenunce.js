import React from 'react';
import { Box } from '@chakra-ui/react';

function CVMessageDetailsDenunce({ model }) {
  return <Box>{JSON.stringify(model)}</Box>;
}

export default CVMessageDetailsDenunce;
