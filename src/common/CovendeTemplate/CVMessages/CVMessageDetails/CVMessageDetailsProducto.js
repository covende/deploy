import React from 'react';
import { Box } from '@chakra-ui/react';

function CVMessageDetailsProducto({ model }) {
  return <Box>{JSON.stringify(model)}</Box>;
}

export default CVMessageDetailsProducto;
